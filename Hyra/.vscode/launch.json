{
    "version": "0.2.0",
    "configurations": [

        {
            "name": "Launch integrated (Mono)",
            "type": "mono",
            "request": "launch",
            "program": "${workspaceRoot}/bin/Debug/Hyra.exe",
            "cwd": "${workspaceRoot}",
            "console":"integratedTerminal"
        }
    ]
}